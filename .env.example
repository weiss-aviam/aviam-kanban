# Supabase Configuration
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-only Supabase key (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Configuration
# Use the pooler connection (port 6543) for both runtime and migrations
# This avoids IPv6 connection issues with direct connection (port 5432)
# Format: postgresql://postgres.[project-ref]:[password]@[region].pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=postgresql://postgres.your-project-ref:your-password@aws-0-region.pooler.supabase.com:6543/postgres?sslmode=require

# Environment
NODE_ENV=development

# App Configuration
APP_URL=http://localhost:3000

# Sub-path hosting (optional)
# Leave empty for root path, or set to "/kanban" for sub-path hosting
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
